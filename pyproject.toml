[project]
name = "respond-benchmarking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "polars>=1.43.2",
    "respondpy",
]

[tool.uv.sources]
respondpy = { path = "../respondpy", editable = true }
